<div class="block-provus-cta">
  <div class="cta-wrapper has-bg-image {{ background_color }}">
    {% if background_image_path %}
      <div class="cta-bg-image-container" style="background-image: url({{ background_image_path.src }})"></div>
    {% else %}
      <div class="cta-bg-image-container"></div>
    {% endif %}

    {% if subtitle|render %}
      <h3 class="cta-sub-title">{{ subtitle }}</h3>
    {% endif %}

    {% if title|render %}
      <h2 class="cta-title">{{ title }}</h2>
    {% endif %}

    {% if body|render %}
      <div class="cta-body">
        {{ body|raw }}
      </div>
    {% endif %}
    
    <div class="cta-links">
      <div class="field field--name-field-provus-links field--type-link field__items">
      {% block cta_footer %}
        {{
          include(
            '@provus_windy_college/components/button/button.twig',
            {
              text: 'Button 1',
              url: 'https://example.com',
              variant: 'primary'
            },
            with_context: false
          )
        }}
        {{
          include(
            '@provus_windy_college/components/button/button.twig',
            {
              text: 'Button 2',
              url: '/page/1',
              variant: 'secondary'
            },
            with_context: false
          )
        }}
      {% endblock %}
      </div>
    </div>
  </div>
</div>